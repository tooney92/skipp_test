json.lens do
  json.id @lens.id
  json.color @lens.color
  json.description @lens.description
  json.prescription_type @lens.prescription_type
  json.lens_type @lens.lens_type
  json.stock @lens.stock
  json.price @lens.price
end
